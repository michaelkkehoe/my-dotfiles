# Prompt with Git branch

prompt_git() {
    local branch_name
    branch_name=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)
    if [ -n "$branch_name" ]; then
        echo "%F{blue}($branch_name)%f"
    fi
}

PROMPT='[%F{green}%n%f@%F{cyan}%m%f:%F{yellow}%~%f] $(prompt_git) %# '